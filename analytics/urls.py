from django.conf.urls import patterns





urlpatterns = patterns('analytics.views',
                            (r'^$', 'dashboard'),   
                                                        
                       )
